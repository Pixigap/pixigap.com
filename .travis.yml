language: node_js
node_js: node
cache: pip
script:
  - sudo pip install s3cmd
  - s3cmd sync --delete-removed --region=us-west-2 -P -M -r . s3://staging.pixigap.com/
notifications:
    email:
        on_failure: always